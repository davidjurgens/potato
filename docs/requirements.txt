# Documentation build requirements (Read the Docs builds from this file).
# Top-level deps only; pip resolves a compatible set. Material ships its own
# search + pymdown-extensions, so no separate pins are needed.
mkdocs>=1.6
mkdocs-material>=9.5
